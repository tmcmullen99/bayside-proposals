name = "bayside-proposal-builder"
compatibility_date = "2024-09-23"
pages_build_output_dir = "public"

# Production environment — set SUPABASE_SERVICE_KEY and ANTHROPIC_API_KEY
# as encrypted secrets in the CF Pages dashboard, not here.

[vars]
# Public vars only. Secrets go in the dashboard.
ENVIRONMENT = "production"
